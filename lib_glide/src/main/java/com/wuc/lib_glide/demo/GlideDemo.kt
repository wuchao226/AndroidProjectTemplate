package com.wuc.lib_glide.demo

/**
 * @author: wuc
 * @date: 2025/5/18
 * @description:
 */
class GlideDemo {

  /*  // 在Activity或Fragment中初始化
    val smartScrollListener = SmartScrollListener(context)
    recyclerView.addOnScrollListener(smartScrollListener)

    // 在Adapter的onBindViewHolder中使用
    override fun onBindViewHolder(holder: ViewHolder, position: Int) {
        val item = dataList[position]
        // 使用智能加载，传入当前滚动速度
        holder.imageView.loadSmartImage(item.imageUrl, smartScrollListener.getScrollSpeed())
    }

    // 在Application或合适的地方预加载关键图片
    override fun onCreate() {
        super.onCreate()
        // 预加载首屏关键图片
        val criticalImages = listOf(
            "https://example.com/image1.jpg",
            "https://example.com/image2.jpg"
        )
        preloadCriticalImages(this, criticalImages)
    }*/
}